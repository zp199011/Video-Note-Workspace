# 视频分析总结项目规则

## 项目定位

把 B 站/抖音短视频沉淀为可追溯的知识资料：保留原始字幕，生成 AI 润色、说话人版、文字大纲、思维导图和总 MD，并通过“资料完整度 100% → Codex 核查合成”写入 Obsidian。

## 启动与技术底座

- 在项目根目录运行 `npm start`；默认访问 `http://127.0.0.1:4173/`，可用 `SUBTITLE_PORT` 改端口。
- 服务端是 Node.js `node:http`，前端是原生 HTML/CSS/JavaScript，数据使用本地 JSON/JSONL。
- 本地 ASR 使用 `workers/` 下的 FunASR/CAM++ 运行环境；模型和媒体缓存属于运行依赖。

## 目录与数据约定

- `server.js`：接口、任务、资料包和导出；`public/`：页面；`workers/`：ASR；`test/`：回归测试；`data/`：用户笔记、任务、日志、媒体和模型。
- 原始字幕、AI 润色版、说话人版、大纲和思维导图分开保存；任何结果不得覆盖原文。
- 资料不完整时 Codex 不得合成；API 负责整理资料，Codex 负责只读核查与合成。翻译功能已移除，历史翻译字段只保留、不进入新流程。
- 不删除或重置 `data/`、模型、媒体、任务和日志；清理前先做清单并取得用户确认。

## 当前状态与推进

- 阶段 1、阶段 2A/2B/2C 已实现并通过内部回归；当前等待用户最终验收，不自动扩展第三阶段。
- 当前回归命令：`node --check server.js`、`node --check public/app.js`、`npm test`、`git diff --check`。
- 任何项目修改后，都要重启服务并打开一个新的本地页面，再报告验证结果。
- 新需求先更新计划和验收边界；发现同一问题连续两次修复失败时，停止猜测，收集真实日志和复现材料。
