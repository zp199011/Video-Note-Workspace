{
  "name": "video-subtitle-lab",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first Bilibili and Douyin video transcription and knowledge workspace.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "setup:asr": "python3.12 -m venv .venv && ./.venv/bin/python -m pip install -r requirements-asr.txt",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
